"""THE LOOP: Act -> Capture -> Watch -> Critique -> Fix -> Re-capture.

Implemented in Milestone 3: Playwright/gdigrab capture, structured-JSON
critic, phash-aligned diff engine, iteration runner with persisted artifacts,
and the before/after proof renderer.
"""
